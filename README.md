# A simple Rails-like server.

Try it out! Run 'ruby bin/demo_server.rb' from your command line and navigate to localhost:3000 in your browser.

This code mimics Rails' basic functionality. Rails Lite runs on a WEBrick server and has ControllerBase, Session, Params, Router and Route classes.
